# Kopeeri failiks .env VÕI kasuta `npm run setup` (soovitatud — parool ei satu ajalukku)
MAIL_USER=gert@leisson.eu
MAIL_PASS=
MAIL_FROM_NAME=Gert Leisson
IMAP_HOST=imap.zone.eu
IMAP_PORT=993
SMTP_HOST=smtp.zone.eu
SMTP_PORT=465
CRM_PORT=4310
PARTNER_API_TOKEN=

# Optional comma-separated addresses owned by you; excluded from customer inquiry counts.
CRM_SELF_EMAILS=
